# run syntha api (optional)
- wd: "/mnt/d/Work/Intuitas/repo/ambo_sim"
  env: "source .venv/bin/activate"
  cmd:
    - "python3 -c 'print(\"Firing up ambo_sim --no-llm --fhir-export\")'"
    - 'python3 app.py --no-llm --fhir-export'
    - 'python3 -c "import time; time.sleep(60)"'
